<div class="container">
  <h1>Authors</h1>
  <% @users.each do |user| %>
  <div class="post">
    <h2><a href="/user/<%= user.id %>"><%= user.name %></a></h2>
    <p>Member for <%= (((Time.now) - (user.created_at))/60/60).round(0) %> hours</p>
  </div>
  <% end %>
</div>
